4.22
好多问题都出在跨域上,解决方法有很多，其中可以后端添加请求头，或者引入cors插件。前端可以配置viet服务代理等。
4.23
后端 req.status()设置在100~500之外的值，会碰到各种各样奇葩的bug。本来有处理的中间件，它找不到，报500错。程序也没报错。
4.24
后端 无法拿到sesion bug，原因是cookie设置的问题，path配置为“/”.
4.25
前端 由于路由配置中 一个路由的name值重复，导致router.addRoute()添加路由不成功（不报错），所以很难排除。